server:
  port: 8888

spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
#   primary:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/testdb?useUnicode=true&characterEncoding=utf-8&useSSL=false  #&serverTimezone=GMT
    username: root
    password: root
#   secondary:
#    driver-class-name: com.mysql.jdbc.Driver
#    jdbc-url: jdbc:mysql://localhost/testdb2?useUnicode=true&characterEncoding=utf-8&useSSL=false  #&serverTimezone=GMT
#    username: root
##    password: root
  jpa:
    hibernate:
     ddl-auto: update
     database: mysql
     show-sql: true

  logging:
   level:
    com.example1.demo1.bootlaunch: debug
